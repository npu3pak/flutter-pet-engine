# demo — приложение-пример pet_engine

Статус: фаза 3 выполнена (12 сентября 2026). Это переписанное приложение
`pet_engine/engine/example` на новом API: все 48 фич восьми групп плюс
демонстрация гизмо движка (49-я фича), диплинки, снимки, визуальные
проверки и стресс-экран.

Назначение:

- показать все возможности движка (полный каталог из 48 фич примера плюс
  сцена гизмо: сцены, камеры, динамика, размещение, уровни, погода,
  эффекты, проекция);
- служить документацией кодом: код максимально нагляден и рассчитан на
  джунов и младших мидлов;
- служить стендом для отладки, визуального тестирования и измерений
  производительности.

Запуск (из корня репозитория):

```bash
fvm dart run scripts/stage_app_assets.dart
cd demo && fvm flutter run -d macos --enable-flutter-gpu --enable-impeller
```

Снимки фич на macOS:

```bash
./tool/deeplink.sh capture <фича> <имя> [delay=мс]
```

Правила:

- в `lib/` нет импортов `package:flutter_scene/...`;
- фичи строятся на публичном API `SceneViewport`/`SceneController`/
  `SceneNode`;
- если фича не выражается через API — это повод доработать API, а не
  обойти его.
