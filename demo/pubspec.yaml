name: demo
description: "demo: приложение-пример pet_engine_v2 — каталог фич, визуальная проверка, документация кодом."
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.13.0-275.0.dev

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  hooks: ^2.0.0
  path: ^1.9.1
  pet_engine_v2:
    path: ../engine
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/pet_project/
    - assets/shaders/
