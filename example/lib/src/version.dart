/// Версия приложения example.
///
/// Должна совпадать с `version` в `example/pubspec.yaml`; совпадение проверяет
/// тест `test/version_test.dart`. Показывается в разделе «О приложении» и
/// записывается в `visual_tests.json` вместе с каждым результатом проверки.
const String kDemoAppVersion = '0.1.0+1';
