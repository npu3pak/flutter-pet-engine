/// Версия приложения scene_editor.
///
/// Должна совпадать с `version` в `scene_editor/pubspec.yaml`; совпадение
/// проверяет тест `test/version_test.dart`. Записывается в
/// `visual_tests.json` вместе с каждым результатом проверки.
const String kSceneEditorAppVersion = '1.0.0+1';
