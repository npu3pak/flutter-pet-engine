# Журнал производительности pet_engine

Замеры и разбор узких мест. Методика: `scene_editor` собирается как обычное
macOS-приложение (`fvm flutter run -d macos --enable-flutter-gpu
--enable-impeller`) и запускается с `--dart-define=pet.perf.drag=true` —
встроенный харнесс `scene_editor/lib/src/perf_drag.dart` открывает
`demo/assets/Pet/model_2` и шлёт настоящие pointer-события, протягивая ось X
гизмо переноса 60 шагов по каждому виду объекта. Печатает sync (обработчик
события целиком) и frame (до конца кадра) p50/p95; полная пересборка сцены
видна в строках `renderer: rebuild ... (Nms)`, снимки — в
`scene_editor/temp/perf_screenshots/`. Харнесс живёт в репозитории на время
перф-прохода (без видеокарты его работа невозможна: сборка геометрии в
движке идёт через GPU), после закрытия перф-фазы удаляется.

Сцена: `Pet/model_2`, 24 объекта, 9335 частей, 120 нод, текстуры загружены.

## 0. Базлайн (13 сентября 2026, до правок)

`sync` — время обработчика одного шага драга; `frame` — ожидание конца кадра
(упор в vsync/GPU). Медианы по 60 шагам.

| Объект | Вид | sync p50 | sync p95 | frame p50 |
|---|---|---|---|---|
| `obj_6` | куб (стена) | **0.63 мс** | 0.80 | 32.1 |
| `obj_13` | вставка модели (кресло) | **53.6 мс** | 92.4 | 24.4 |
| `csg_1` | CSG-результат | 0.75 мс | 2.73 | 27.7 |
| `obj_4` | спрайт | **53.6 мс** | 90.9 | 23.1 |
| `obj_14` | glTF (кот) | **52.9 мс** | 91.6 | 23.0 |

Полная `ModelRenderer.rebuild`: 49–121 мс на эту сцену (типично 50–60 мс с
загруженными текстурами).

Выводы:

1. **Куб** идёт быстрым путём `refreshObjectTransforms` — 0.6 мс, это норма.
2. **Вставка, спрайт, glTF** пересобирают всю сцену на КАЖДЫЙ шаг драга
   (`editor_scene.dart` `_refreshAfterTransform`: «запечённые» виды вызывают
   `controller.rebuild()` → `ModelRenderer.rebuild`) — 53 мс синхронно, это
   и есть «очень медленное обновление сцен при перемещении».
3. **CSG**: шаг быстрый (0.75 мс), но видимый результат не двигается во
   время драга — `moveExpansion` возвращает операнды (обычные кубы), поэтому
   редактор считает выборку «не запечённой» и обновляет только скрытые ноды
   операндов; результат перестраивается лишь на отпускании. Это баг
   корректности, а не только производительности.
4. Попутно найден баг ввода: `SceneController.hitGizmo` проверял и невидимые
   гизмо — в режиме переноса скрытый гизмо вращения перехватывал клики по
   оси X (исправлено фильтром `visible`, тест в `engine/test/api/gizmo_test.dart`).

## 1. После правок (13 сентября 2026)

Сделано:

- **Трансформационный быстрый путь.** `ModelRenderer.updateObjectTransforms`
  принимает `translated: {elementId: мировой шаг}`: узлы элемента сдвигаются
  приписыванием перевода к текущей локальной матрице — без геометрии, CSG,
  материалов и пересборки. Редакторский `_moveObjects` считает фактический
  (снапнутый) шаг по каждому объекту и передаёт его; для CSG результат
  сдвигается шагом первого операнда (его ноды зарегистрированы под id
  операции). Цепочки билбордов внутри вставок сдвигаются вместе с нодами,
  иначе покадровый разворот возвращал бы спрайты назад.
- **Идемпотентный свет.** `applyLighting` сравнивает подпись конфигурации и
  не пересобирает свет при том же документе; `setShadows`/`setSsao`/
  `setEnvironmentIntensity` — no-op при том же значении. Редактор зовёт
  `_applyLighting` на каждую ревизию — теперь это сравнение строк.
- **Wireframe по перемещённым.** `_refreshWireframes(only:)` обновляет только
  ноды перемещённых элементов (полный проход остался для `rebuild`).
- **Счётчик пересборок.** `SceneController.rebuildCount` — диагностика и
  регрессионный тест: перенос вставки не растит счётчик
  (`scene_editor/test/editor_scene_test.dart`).

| Объект | Вид | sync p50 до | sync p50 после | frame p50 после |
|---|---|---|---|---|
| `obj_6` | куб (стена) | 0.63 мс | 0.49 мс | 32.6 |
| `obj_13` | вставка модели (кресло) | 53.6 мс | **0.44 мс** | 20.1 |
| `csg_1` | CSG-результат | 0.75 мс (не двигался) | 0.71 мс (двигается) | 25.9 |
| `obj_4` | спрайт | 53.6 мс | **0.33 мс** | 19.0 |
| `obj_14` | glTF (кот) | 52.9 мс | **0.34 мс** | 18.6 |

Полных пересборок за время драга — 0 по всем видам (было: одна на шаг у
вставок, спрайтов и glTF). Визуальная проверка: снимки
`scene_editor/temp/perf_screenshots/{before,after}_*` — кресло, CSG-стена,
спрайт окна и glTF двигаются живьём, контур выделения следует за объектом.

Осталось на будущее (не в этом проходе): поворот «запечённых» видов всё ещё
идёт через полную пересборку (перенос — нет); коалесинг pointer-событий до
одного на кадр (sync и так 0.5 мс); инкрементальные оверлеи. Все три пункта
закрыты ниже (13 сентября 2026).

## Поворот объектов без пересборки сцены (13 сентября 2026)

Продолжение перф-прохода: поворот гизмо «запечённых» видов (вставки моделей,
glTF, скругления) пересобирал сцену на каждый шаг. Харнесс расширен режимом
поворота (`PERF-ROT`, 60 шагов по экранной окружности кольца) и печатью
числа полных пересборок за драг.

Сделано:

- **Движок.** `ModelRenderer.updateObjectTransforms` принимает
  `rotated: {id: Matrix4}` — шаг `R_new·R_old⁻¹` в модельном кадре; движок
  сам строит мировой префикс `T(anchor)·Δ·T(−anchor)` и применяет его к
  нодам элемента и цепочкам вложенных билбордов. Для вершин, запечённых с
  зеркалом X (скругления, CSG), префикс сопрягается `M·Δ·M`; для
  вставок/glTF — без сопряжения (их поворот стоит в цепочке после
  зеркального якоря). `SceneController.refreshObjectTransforms` получил
  `rotated`, wireframe обновляется по объединению id.
- **Редактор.** `_rotateObjects` считает фактический снапнутый шаг и идёт
  быстрым путём; `beginRotateDrag` раскрывает выборку через
  `moveExpansion`; скрытый операнд CSG по-прежнему уходит в полный rebuild.
- **Тесты.** `engine/test/fast_rotate_test.dart` (префиксы для солидов,
  вставок, скругления с зеркалом, сдвиг билборд-цепочек);
  `scene_editor/test/editor_scene_test.dart` — «rotating a model instance
  does not rebuild the scene».

| Объект | Вид | sync p50 до | sync p50 после | rebuilds до | rebuilds после |
|---|---|---|---|---|---|
| `obj_6` | куб (стена) | 0.34 мс | 0.32 мс | 0 | 0 |
| `obj_13` | вставка модели (кресло) | **53.5 мс** | **0.30 мс** | 59 | **0** |
| `obj_14` | glTF (кот) | **52.7 мс** | **0.30 мс** | 59 | **0** |

Снимки `scene_editor/temp/perf_screenshots/rot_{before,after}_*` совпадают:
быстрый путь даёт ту же картинку, что и полная пересборка.

## Инкрементальные оверлеи и коалесинг pointer-событий (13 сентября 2026)

Третья часть перф-прохода. До правок каждый шаг драга объекта пересобирал
оверлеи трижды: `_moveObjects` → `rebuildOverlays`, затем microtask
AppState → `editor.rebuild` (ревизия) → `rebuildOverlays`, и ещё раз
безусловный `rebuildOverlays` в `_onAppChanged`. Сетка, рамка, курсор,
контур, мета-слой и гизмо света пересоздавались целиком, покадровый контур
билборда пересобирался даже при неподвижной камере, а итератор
`_selectionHasBillboard` рекурсивно резолвил вставки каждый кадр.

Сделано:

- **Кэш слоёв.** Сетка, рамка и курсор — долгоживущие ноды, пересоздаются
  только при смене модели/размера; курсор двигается трансформом; клетка
  кисти появляется только при арме. Контур выделения — одна нода: перенос
  сдвигает её трансформом (при общем шаге), поворот/смена выделения
  пересобирают только контур. Билборд-контур обновляется по смене yaw, а не
  каждый кадр; `hasBillboard` кэшируется на смене выделения.
- **Дедупликация.** `EditorScene.syncDocument` не пересобирает оверлеи, если
  ревизию уже синкнул драг-путь; `EditorViewport._onAppChanged` вызывает
  полный rebuild только при смене модели или режима, иначе дешёвый
  `syncUiState`.
- **Точечные синки драгов.** Перенос/поворот обновляют контур и гизмо;
  драг меты — только мета-слой; драг света — только гизмо источников.
- **Коалесинг.** Позиция гизмо-драга копится в `EditorScene.queueGizmoDrag`
  и применяется раз в кадр (`flushGizmoDrag` в `update`), с немедленным
  применением на отпускании и отмене; вьюпорт получил обработчик
  `onPointerCancel` (раньше отмена ОС оставляла драг висеть).

| Объект | Вид | sync p50 до | после поворота | после оверлеев | frame p50 |
|---|---|---|---|---|---|
| `obj_6` | куб (стена) | 0.49 мс | 0.32 | **0.14** | 16.6 |
| `obj_13` | вставка модели (кресло) | 0.44 мс | 0.30 | **0.13** | 16.4 |
| `csg_1` | CSG-результат | 0.71 мс | — | **0.14** | 16.2 |
| `obj_4` | спрайт | 0.33 мс | — | **0.11** | 16.7 |
| `obj_14` | glTF (кот) | 0.34 мс | 0.30 | **0.12** | 16.6 |

Поворот после перевода контура на трансформ/точечную пересборку: `obj_13`
0.10 мс, `obj_14` 0.10 мс (было 53.5/52.7 мс, 59 пересборок). Полных
пересборок сцены за драги — 0.

Тесты: `editor_scene_test.dart` («overlay caches survive rebuilds…»,
сдвиг контура без пересборки), `editor_viewport_test.dart` («отмена
указателя завершает драг гизмо»), `overlay_layers_test.dart` (точечные
синки мета-слоя и гизмо света). Проверки: `engine` 515, `demo` 164,
`scene_editor` 355, analyze чист.

Хвост закрыт в той же ветке: мета-слой получил `MetaOverlayLayer.sync`
(сдвиг только нод перемещённой меты; смена формы/подписи пересобирает
только её; удалённая мета убирает свои ноды, чужой кэш текстур цел), гизмо
света — `LightGizmoLayer.sync(only:)` (драг обновляет трансформы только
своего источника; смена типа пересобирает его части). Харнесс расширен
бенчами `PERF-LIGHT` и `PERF-META`:

| Субъект | Вид | sync p50 | frame p50 | rebuilds |
|---|---|---|---|---|
| `light_1` | направленный источник | **0.10 мс** | 16.5 | 0 |
| новая мета | маркер | **0.10 мс** | 16.5 | 0 |



## Стресс уровневого слоя из приложения-примера (13.09.2026)

Стенд `demo`, macOS debug (Impeller), размер 50×50 (2500 клеток).

| Метрика | Значение |
|---|---|
| Сборка `ConstructionModel` (BuildOps) | 7 мс |
| Запекание `LevelBaker.bake` | 2636 мс |
| Элементов | 5200 |
| Слитых мешей (merge) | 3 |
| Пакетных мешей (batch) | 0 |
| Отдельных узлов | 0 |
| Вершин / треугольников | 124800 / 62400 |
| FPS (macOS) | 60.0 |
