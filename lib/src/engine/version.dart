/// Версия пакета `pet_engine` — открытая константа для приложений.
///
/// Должна совпадать с `version` в `pubspec.yaml`; совпадение проверяет
/// тест `test/version_test.dart`. Записывается в журналы визуальных
/// проверок приложений (example, scene_editor), чтобы по замечанию было видно,
/// на какой версии движка оно сделано.
const String kPetEngineVersion = '0.1.0-dev.3';
